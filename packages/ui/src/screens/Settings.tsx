/**
 * Placeholder. The real Settings screen (tracking snippet, project details,
 * etc.) lands in a later task, which replaces this component and updates
 * `Router.test.tsx`'s assertions in the same commit. This exists only so
 * `/settings` has somewhere to route to.
 */
export function Settings() {
  return <h1 className="text-lg font-semibold">Settings</h1>
}
