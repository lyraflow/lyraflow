# Lyraflow Documentation

Public product documentation: installation, self-hosting guides, concepts, and API reference will live here as the product takes shape.

For v0.1 the whole public surface is small enough to live in the top-level [README](../README.md): installing the stack, creating a project, the four ingest endpoints with their payload shape, and identity resolution (`/v1/alias`, `GET /v1/persons/:id`).
